@*@using RealBranch.Controllers
@using System.Linq
@using Model
@model AppUser

@{ ViewBag.Title = "Пользовательские свойства"; }

<div class="panel panel-primary">
    <div class="panel-heading">
        Пользовательские свойства
    </div>
    <table class="table table-striped">
        <tr><th>Текущий город</th><td>@HomeController.GetCityName(Model.Status)</td></tr>
    </table>
</div>

@using (Html.BeginForm())
{
    <div class="form-group">
        <label>Город: </label>
        @Html.DropDownListFor(x => x.Status, new SelectList(
                Enum.GetValues(typeof(Status))
                .OfType<Status>()
                  .Select(c =>
                  {
                      return new
                      {
                          Id = c,
                          Text = HomeController.GetCityName(c)
                      };
                  }),
                "Id", "Text"
        ))
    </div>
    <button class="btn btn-primary" type="submit">Сохранить</button>
}*@